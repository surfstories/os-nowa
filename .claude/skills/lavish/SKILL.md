---
name: lavish
description: Turn what you are looking at into an HTML page opened in the browser, so the user can annotate the exact element or phrase and send the notes back. Use when the user says "draw this", "show me this visually", "make a page out of it", or the same in any other language.
---

# lavish

**Read `system/procedures/lavish.md` and follow it.** That file is the method: what this does, the
house rules that override the vendored instructions, and the vendored text itself.

This file is a pointer and nothing else. It does not restate the method, because a method written in
two places gets updated in one of them.

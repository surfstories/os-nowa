# Maintaining OS_Nowa - rules for changing the engine

These rules bind whoever changes the engine: the files listed as engine paths in `AGENTS.md`, under
"Engine and data". They do not bind a session working in someone's installed workspace. Those rules
are in `AGENTS.md` and they are a different job: there, the folder belongs to the user and almost
nothing may be touched without asking. Here, the folder is the product.

Nothing in this file is loaded automatically. Read it when you are about to change the engine.

1. **One commit, one topic.** If the message needs "and", it is two commits. No "WIP", "checkpoint"
   or end-of-day snapshot commits: a mixed commit cannot be reverted without taking unrelated work
   with it.

2. **A commit message never adds the agent as an author.** No co-author trailer, no "generated by"
   line. The person who approved the change is its author.

3. **Every engine change is acceptance-tested before it lands.** Clone the branch to a fresh folder,
   open a new session in it, and run the affected path the way a new user would: paste the install
   prompt, say "set me up", say "check my system". The PR body records what was run and what
   happened. Reading the diff is not a test of a system whose whole failure mode is invisible from
   disk.

4. **Never test always-loaded context from disk.** A Tier 1 change is verified by asking a fresh
   session whether there is a diagnostic token in the instructions it was given, and to quote it.
   **Never name the token in the question**: a prompt containing it is answered by itself, whether or
   not anything loaded. Correct on disk and never delivered is the failure this rule exists to catch,
   and it is the one failure a diff cannot show. The reasoning is in `system/tiers.md`.

5. **Editing a rule means editing every copy of it in the same commit.** Before you change a rule's
   wording, grep the repository for it. A rule that lives in two files will be updated in one of
   them, and nothing warns you: each file reads as correct on its own, and only a reader holding
   both at once sees the conflict.

6. **The em dash ban is hard rule 6 in `AGENTS.md`, and it binds engine text too.** New or rewritten
   lines use a plain hyphen. Lines written before the rule keep their dashes: do not sweep them, and
   do not treat them as precedent.

7. **Prefer quality, simplicity, robustness and long-term maintainability over what a change costs
   to make.** Build cost is the cheapest input here and the one that matters least in six months.
   This is a rule about the engine. Inside a user's workspace, `system/procedures/level-up.md`
   deliberately prefers the smallest thing that works, and that stays.

8. **A defect you find while doing something else gets fixed, not stepped over.** A stale count, a
   dead pointer, a placeholder that survived: fix it in its own commit if it is not the topic you
   are on, and never leave it unrecorded because it was not your fault.

9. **Never hand-edit output a procedure is supposed to produce.** An index row, a log line, a filled
   template: if it is wrong, the procedure that writes it is wrong. Fix the procedure and let it
   write the file again.

## Why this file exists

Every other rule in this repository addresses the person using an installed workspace. Until this
file, nothing addressed the person changing the engine, which is where the defects actually came
from: four found by independent acceptance testing in one pass, a sentinel that never reached the
model because it was written inside an HTML comment, and an installed folder that stayed wired to
this repository.

None of those was a failure of the rules in `AGENTS.md`. They were failures of the work that changes
them, done without any rules at all.
